apiVersion: v1
kind: Pod
metadata:
  name: toolbox
spec:
  containers:
    - name: toolbox
      image: alpine:3.20
      command: ["sh", "-c", "sleep 36000"]
      stdin: true
      tty: true
  restartPolicy: Never
